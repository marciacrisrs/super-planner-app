name: Autonomous Merge

# TEMPORARILY DISABLED
# Re-enable the event triggers after PR #374 has passed all CI and review gates.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Autonomous merge is temporarily disabled."
