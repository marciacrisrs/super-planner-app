# UX 10 — Wave 2 Implementation

The Agora now frames the next action as a suggestion and keeps the person's choice explicit. Copy avoids implying that the system owns the decision, while the card retains a clear primary action and low-friction alternatives.

This wave changes presentation and interaction language only. Planning-engine constraints and decisions remain domain-owned.
